const Underline = () => {
  return (
    <section id='underline'>
      <h2>2. Подчеркивание текста при ховере:</h2>
      <p>
        Есть блок с текстом(например тег p). Сделать так, чтобы при ховере строки текста постепенно подчеркивались линией. Данная анимация должна происходить при любом размере текста. Использовать теги внутри тега &lt;p&gt; не запрещается.
      </p>
    </section>
  );
}

export default Underline;